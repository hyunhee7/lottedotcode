<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="members">
	<select id="isValid" parameterType="membersDto" 
			resultType="membersDto">
		SELECT id,pwd,imagePath
		FROM members
		WHERE id=#{id} AND pwd=#{pwd}
	</select>
	<select id="isExistId" parameterType="string" 
				resultType="string">
		SELECT id 
		FROM members
		WHERE id=#{id}
	</select>	
	<insert id="insert" parameterType="membersDto">
		INSERT INTO members
		(id,pwd,name,imagePath)
		VALUES(#{id},#{pwd},#{name},#{imagePath})
	</insert>
	<select id="getPath" parameterType="string" 
				resultType="string">
		SELECT imagePath 
		FROM members
		WHERE id=#{id}
	</select>		
</mapper>








